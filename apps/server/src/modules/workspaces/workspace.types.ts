/**
 * Workspace Module Types
 * 
 * Backend-only types for workspace module
 * Shared types are in @velora/types package
 */

// Re-export shared types for convenience
export {
  WorkspaceRole,
  WorkspaceMember,
  Workspace,
  CreateWorkspaceDTO,
  UpdateWorkspaceDTO,
  AddMemberDTO,
  UpdateMemberRoleDTO,
  WorkspaceResponse,
  WorkspaceMemberResponse,
} from '@velora/types';
